# Módulos
# Podemos utilizar bibliotecas externas através do import
import math
n = int(input('Insira um número: '))
raiz = math.sqrt(n)

# Podemos utilizar apenas um comando de uma biblioteca
from math import ceil
ceil = ceil(n)
